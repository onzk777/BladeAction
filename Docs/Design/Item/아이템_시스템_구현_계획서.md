# 아이템 시스템 구현 계획서

## 문서 목적
이 문서는 아이템 시스템의 구현 순서와 각 단계별 상세 작업 내용을 정의합니다. 탑다운 방식으로 핵심 코어 모듈을 우선 구현하여 안정적인 시스템 구축을 목표로 합니다.

---

## 시스템 아키텍처 개요

### 계층 구조 (Hierarchy)
```
Layer 1: 타입 시스템 (ScriptableObject)
    ├── WeaponTypeData
    ├── ArmorTypeData
    ├── AccessoryTypeData
    └── SwordplayStyleData
         ↓
Layer 2: 스탯 및 데이터 시스템
    ├── EquipmentStats (구조체)
    ├── StatTable (ScriptableObject)
    └── StatDatabase (중앙 관리)
         ↓
Layer 3: 아이템 데이터 시스템
    ├── ItemData (하이브리드 구조)
    └── ItemDatabase (중앙 관리)
         ↓
Layer 4: Excel 데이터 관리
    ├── CSV Importer
    ├── Data Mapper
    └── Validation System
         ↓
Layer 5: 인벤토리 코어
    ├── CombatantInventory
    ├── OwnedItem
    └── EquipmentSlot
         ↓
Layer 6: 인벤토리 매니저
    ├── InventoryManager (로직)
    ├── ItemEvents (이벤트)
    └── SaveData (저장/로드)
         ↓
Layer 7: UI 시스템
    ├── InventoryUI (메인 패널)
    ├── ItemSlotUI (개별 슬롯)
    ├── EquipmentSlotUI (장비 슬롯)
    └── ItemDetailPanel (정보 패널)
         ↓
Layer 8: 고급 기능
    ├── Filter & Sort
    ├── Drag & Drop
    └── Context Menu
```

### 핵심 설계 원칙
- **데이터 중심 설계**: ScriptableObject 기반 타입 시스템
- **하이브리드 데이터 관리**: Unity Asset (참조) + Excel (텍스트/수치)
- **명확한 책임 분리**: 데이터 / 로직 / UI 레이어 분리
- **확장성**: 새로운 아이템 타입 추가 용이
- **null 안전성**: 모든 참조에 대한 null 체크

---

## 전체 구현 단계 개요

```
Phase 1: 핵심 데이터 구조 (타입 시스템) ✅ 100%
    ↓
Phase 2: 아이템 데이터 시스템 ✅ 100%
    ↓
Phase 3: Excel 데이터 관리 시스템 ✅ 100%
    ↓
Phase 4: 인벤토리 코어 시스템 ✅ 100% (완료)
    ↓
Phase 5: 인벤토리 UI 시스템 🚧 0%
    ↓
Phase 6: 고급 기능 (필터링, 정렬, 드래그앤드롭) 🚧 0%
```

### 전체 진행률: **67%** (Phase 1-4 완료)

---

## Phase 1: 핵심 데이터 구조 (타입 시스템)

### 목표
아이템 시스템의 기반이 되는 타입 시스템을 ScriptableObject로 구현합니다. 모든 아이템이 참조할 타입 데이터를 먼저 구축합니다.

### 1.1 EquipmentStats 구조체 구현
**신규 파일**: `Assets/Script/Item/EquipmentStats.cs`

#### 작업 내용
- 공격, 막기, 쳐내기, 생존 관련 스탯 필드 정의
- 연산자 오버로딩 (+ 연산자)으로 스탯 합산 기능

#### 검증 사항
- [ ] Inspector에서 모든 필드 표시 확인
- [ ] Tooltip이 정상적으로 표시되는지 확인
- [ ] 연산자 오버로딩이 정상 작동하는지 테스트

---

### 1.2 WeaponTypeData 구현
**신규 파일**: `Assets/Script/Item/WeaponTypeData.cs`

#### 작업 내용
- WeaponCategory enum (Melee, Ranged, Magic)
- 기본 정보: typeName, typeKey, typeIcon, description
- 무기 특성: category, baseAttackSpeed, baseRange, requiredLevel

#### 검증 사항
- [ ] CreateAssetMenu로 WeaponType 생성 가능
- [ ] Inspector에서 모든 필드 설정 가능
- [ ] 아이콘 할당 정상 작동

---

### 1.3 ArmorTypeData 구현
**신규 파일**: `Assets/Script/Item/ArmorTypeData.cs`

#### 작업 내용
- ArmorCategory enum (Light, Medium, Heavy, Special)
- 기본 정보 및 방어구 특성 (무게, 기동성)

---

### 1.4 AccessoryTypeData 구현
**신규 파일**: `Assets/Script/Item/AccessoryTypeData.cs`

#### 작업 내용
- AccessoryCategory enum (Ring, Necklace, Bracelet, Special)
- 최대 장착 개수 설정

---

### 1.5 SwordplayStyleData 구현
**신규 파일**: `Assets/Script/Item/SwordplayStyleData.cs`

#### 작업 내용
- 유파 정보 및 요구 무기 타입
- 사용 가능한 ActionCommandData 리스트

#### 검증 사항
- [ ] 모든 타입 데이터 생성 가능
- [ ] 타입 간 참조 정상 작동 (SwordplayStyle → WeaponType)
- [ ] 기본값이 올바르게 설정되는지 확인

---

## Phase 2: 아이템 데이터 시스템

### 목표
타입 시스템을 기반으로 실제 아이템 데이터를 관리하는 구조를 구축합니다.

### 2.1 ItemType Enum 정의
**신규 파일**: `Assets/Script/Item/ItemType.cs`

### 2.2 StatTable 및 StatDatabase 구현
**신규 파일**: `Assets/Script/Item/StatTable.cs`, `StatDatabase.cs`

#### 작업 내용
- StatTable: tableKey, stats, description
- StatDatabase: 스탯 테이블 중앙 관리 및 검색 기능

---

### 2.3 ItemData 구현 (하이브리드 구조)
**신규 파일**: `Assets/Script/Item/ItemData.cs`

#### 작업 내용
- Unity Asset References: icon, appearance, weaponType, armorType, accessoryType, swordplayStyle, statTable
- Excel Data: itemKey, itemName, description, itemType, requiredLevel, baseValue, maxStack
- GetStats() 메서드 (null-safe)

#### 검증 사항
- [ ] ItemData 생성 가능
- [ ] Excel Data 필드가 Inspector에서 편집 가능
- [ ] Asset 참조가 정상 작동
- [ ] GetStats() 메서드 null 처리 확인

---

### 2.4 ItemDatabase 구현
**신규 파일**: `Assets/Script/Item/ItemDatabase.cs`

#### 작업 내용
- 타입 데이터 및 아이템 데이터 중앙 관리
- GetItem(), GetItemsByType(), GetWeaponsByType() 메서드

#### 검증 사항
- [ ] ItemDatabase 생성 및 아이템 추가 가능
- [ ] 검색/필터링 메서드 정상 작동
- [ ] null 처리 확인

---

## Phase 3: Excel 데이터 관리 시스템

### 목표
CSV/Excel 파일에서 아이템 데이터를 읽어와 Unity Asset과 매핑하는 시스템을 구축합니다.

### 3.1 ExcelRowData 구조체
**신규 파일**: `Assets/Script/Item/Excel/ExcelRowData.cs`

### 3.2 CSVReader 구현
**신규 파일**: `Assets/Script/Item/Excel/CSVReader.cs`

#### 작업 내용
- CSV 파일 읽기 및 파싱
- 헤더 건너뛰기 및 데이터 검증

---

### 3.3 ItemDataMapper 구현
**신규 파일**: `Assets/Script/Item/Excel/ItemDataMapper.cs`

#### 작업 내용
- Excel 데이터를 ItemData에 매핑
- ItemType 문자열 파싱

---

### 3.4 ItemDataImporter 에디터 윈도우
**신규 파일**: `Assets/Script/Item/Editor/ItemDataImporter.cs`

#### 작업 내용
- CSV 파일 선택 및 로드
- Database 참조 설정
- Import to Unity 기능
- 로드된 데이터 미리보기

#### 검증 사항
- [ ] CSV 파일 로드 정상 작동
- [ ] Excel 데이터가 ItemData로 정상 매핑
- [ ] 새 아이템 생성 및 기존 아이템 업데이트 정상 작동
- [ ] Database에 아이템 추가 확인

---

## Phase 4: 인벤토리 코어 시스템 ✅ **완료: 100%**

### 목표 ✅
아이템을 저장하고 관리하는 인벤토리 핵심 로직을 구현합니다.

### 4.1 OwnedItem 클래스 ✅ **완료: 100%**
**파일**: `Assets/Script/Item/OwnedItem.cs`

#### 구현된 내용
- itemKey, quantity, maxQuantity, isEquipped, isLocked, acquiredTime
- AddQuantity(), RemoveQuantity(), SetQuantity() 메서드
- GetItemData(), UpdateMaxQuantity() 메서드
- IsValid(), IsEmpty(), IsFull() 유효성 검사

#### 완료된 개선사항
- **maxStack=0 문제 해결**: UpdateMaxQuantity()에서 최소값 1 보장

---

### 4.2 EquipmentSlot 클래스 ✅ **완료: 100%**
**파일**: `Assets/Script/Item/EquipmentSlot.cs`

#### 구현된 내용
- EquipmentSlotType enum (Weapon, Armor, Accessory, SwordArtStyle)
- slotType, slotName, isActive, equippedItemKey, equippedQuantity
- EquipItem(), UnequipItem(), CanEquipItem() 메서드
- IsEmpty(), IsAvailable() 상태 확인

#### 완료된 개선사항
- **타입 호환성 검증**: 아이템 타입과 슬롯 타입 매칭 시스템

---

### 4.3 CombatantInventory 클래스 ✅ **완료: 100%**
**파일**: `Assets/Script/Item/CombatantInventory.cs`

#### 구현된 내용
- maxItemSlots (5000개), inventoryName 설정
- items (List<OwnedItem>), equipmentSlots (List<EquipmentSlot>) 관리
- InitializeDefaultEquipmentSlots(): 장신구 5개 포함 총 8개 슬롯 생성
- AddItem(), RemoveItem(), GetItemQuantity(), HasItem() 아이템 관리
- EquipItem(), UnequipItem(), GetEquippedItem() 장비 관리
- FindItems(), GetItemsByType() 검색 기능
- CleanupInventory(), ClearInventory() 유틸리티

#### 완료된 개선사항
- **메모리 최적화**: ItemDatabase.Instance 캐싱 시스템으로 반복 로드 방지
- **에디터 호환성**: SafeTriggerEvent 메서드로 안전한 이벤트 처리

#### 검증 완료 ✅
- [x] Initialize() 호출 시 기본 슬롯 생성 확인 (장신구 5개 포함)
- [x] 아이템 추가/제거 정상 작동
- [x] 스택 가능 아이템 처리 확인
- [x] 장착/해제 정상 작동

---

### 4.4 ItemEvents 시스템 ✅ **완료: 100%**
**파일**: `Assets/Script/Item/ItemEvents.cs`

#### 구현된 내용
- 싱글톤 패턴으로 이벤트 중앙 관리
- 7가지 이벤트: ItemAdded, ItemRemoved, ItemEquipped, ItemUnequipped, ItemQuantityChanged, InventoryFull, InventoryCleared
- ItemEventData 구조체로 이벤트 정보 전달
- 디버그 로그 시스템 포함
- CombatantInventory와 자동 연동

#### 완료된 개선사항
- **에디터 모드 호환성**: DontDestroyOnLoad 조건부 처리 (Application.isPlaying 체크)
- **안전한 이벤트 처리**: 모든 이벤트 타입 정상 발동 확인

#### 검증 완료 ✅
- [x] **테스트 및 검증**: ItemSystemTestRunner를 통한 5단계 검증 완료

---

### 4.5 ItemSystemTestRunner 검증 시스템 ✅ **완료: 100%**
**파일**: `Assets/Script/Item/ItemSystemTestRunner.cs`

#### 구현된 내용
- **5단계 순차 검증 시스템**: 기본 구조 → 아이템 관리 → 수량 관리 → 장비 슬롯 → 이벤트 시스템
- **Context Menu 지원**: Unity 에디터에서 직접 실행 가능한 테스트 메뉴
- **상세한 디버그 로그**: 각 단계별 진행 상황과 결과를 명확하게 구분하여 표시

#### 검증 단계별 결과
1. **1단계**: CombatantInventory 기본 구조 검증 ✅
2. **2단계**: 아이템 관리 기능 검증 (추가/제거) ✅  
3. **3단계**: OwnedItem 수량 관리 검증 ✅
4. **4단계**: EquipmentSlot 장비 슬롯 검증 ✅
5. **5단계**: ItemEvents 이벤트 시스템 검증 ✅

#### 주요 문제 해결 과정
- **maxStack=0 문제**: UpdateMaxQuantity()에서 최소값 1 보장으로 해결
- **에디터 모드 호환성**: ItemEvents DontDestroyOnLoad 조건부 처리
- **메모리 최적화**: ItemDatabase.Instance 캐싱 시스템 도입
- **하드코딩 의존성 제거**: Resources.LoadAll을 사용한 유연한 데이터베이스 검색

---

## Phase 5: 인벤토리 UI 시스템 🚧 **진행률: 0%**

### 목표
플레이어가 인벤토리를 조작할 수 있는 UI를 구현합니다.

### 5.1 ItemSlotUI 구현
**신규 파일**: `Assets/Script/Item/UI/ItemSlotUI.cs`

#### 작업 내용
- iconImage, quantityText, backgroundImage, highlightImage
- Setup(), Clear(), UpdateDisplay()
- SetSelected(), OnPointerClick()

---

### 5.2 EquipmentSlotUI 구현
**신규 파일**: `Assets/Script/Item/UI/EquipmentSlotUI.cs`

#### 작업 내용
- slotNameText, iconImage, emptySlotIcon
- Setup(), UpdateDisplay(), OnPointerClick()

---

### 5.3 InventoryUI 메인 패널
**신규 파일**: `Assets/Script/Item/UI/InventoryUI.cs`

#### 작업 내용
- panel, itemGridContainer, equipmentSlotContainer
- Initialize(): 인벤토리 및 UI 초기화
- CreateEquipmentSlots(), RefreshItemGrid()
- OnItemSlotClicked(), OnEquipmentSlotClicked()
- RefreshAll(): 전체 UI 갱신
- TogglePanel(): 패널 열기/닫기

#### 검증 사항
- [ ] UI 정상 생성 확인
- [ ] 아이템 슬롯 클릭 및 선택 작동
- [ ] 장비 슬롯 클릭 및 장착/해제 작동
- [ ] UI 갱신 정상 작동
- [ ] **실제 테스트 가능**: 플레이어가 직접 조작 가능한 수준

---

## Phase 6: 고급 기능 🚧 **진행률: 0%**

### 목표
필터링, 정렬, 드래그앤드롭 등 사용성을 향상시키는 기능을 구현합니다.

### 6.1 ItemFilter 및 정렬 시스템
**신규 파일**: `Assets/Script/Item/ItemFilter.cs`

#### 작업 내용
- SortType enum (Name, Quantity, AcquiredTime, Level, Type)
- ItemFilter: itemType, searchKeyword, minLevel, maxLevel, showEquipped
- Apply() 메서드: 필터링 로직
- ItemSorter.Sort(): 정렬 로직

---

### 6.2 InventoryManager (통합 관리)
**신규 파일**: `Assets/Script/Item/InventoryManager.cs`

#### 작업 내용
- Singleton 패턴
- itemDatabase, statDatabase 참조
- inventory, currentFilter, currentSortType
- GetFilteredItems(): 필터링 및 정렬 적용
- AddItem(), RemoveItem(): 아이템 관리 (이벤트 발행 포함)

---

## 구현 순서 요약

### Phase 1: 핵심 데이터 구조 (2-3일 예상)
- EquipmentStats 구조체
- WeaponTypeData, ArmorTypeData, AccessoryTypeData, SwordplayStyleData
- 타입 에셋 생성 및 테스트

### Phase 2: 아이템 데이터 시스템 (2일 예상)
- ItemType, StatTable, StatDatabase
- ItemData (하이브리드 구조)
- ItemDatabase
- 기본 아이템 에셋 생성

### Phase 3: Excel 데이터 관리 (3-4일 예상)
- CSV Reader
- Data Mapper
- ItemDataImporter 에디터 윈도우
- CSV 파일 작성 및 임포트 테스트

### Phase 4: 인벤토리 코어 (2-3일 예상)
- OwnedItem, EquipmentSlot
- CombatantInventory
- ItemEvents
- 로직 테스트

### Phase 5: 인벤토리 UI (3-4일 예상)
- ItemSlotUI, EquipmentSlotUI
- InventoryUI 메인 패널
- UI Prefab 제작
- UI 연동 테스트

### Phase 6: 고급 기능 (2-3일 예상)
- ItemFilter 및 ItemSorter
- InventoryManager 통합
- 필터/정렬 UI 추가
- 전체 시스템 통합 테스트

**총 예상 기간**: 14-19일

---

## 검증 체크리스트

### Phase 1 완료 검증
- [ ] 모든 타입 ScriptableObject 생성 가능
- [ ] EquipmentStats 연산자 정상 작동
- [ ] Inspector에서 모든 필드 설정 가능

### Phase 2 완료 검증
- [ ] ItemData 생성 및 설정 가능
- [ ] ItemDatabase 검색/필터링 정상 작동
- [ ] StatDatabase에서 스탯 조회 가능

### Phase 3 완료 검증
- [ ] CSV 파일 로드 정상 작동
- [ ] Excel 데이터가 ItemData로 매핑
- [ ] 신규/기존 아이템 처리 확인

### Phase 4 완료 검증 ✅
- [x] 인벤토리 초기화 정상 작동
- [x] 아이템 추가/제거 정상 작동
- [x] 장착/해제 정상 작동
- [x] 이벤트 발행 확인 (7가지 이벤트 타입 모두 검증)

### Phase 5 완료 검증
- [ ] UI 정상 생성 및 표시
- [ ] 아이템 선택 작동
- [ ] 장비 슬롯 상호작용 작동
- [ ] UI 갱신 정상 작동

### Phase 6 완료 검증
- [ ] 필터링 정상 작동
- [ ] 정렬 정상 작동
- [ ] InventoryManager 통합 완료
- [ ] 전체 시스템 안정성 확인

---

## 주의사항

### 1. null 안전성
- 모든 Asset 참조는 null 체크 필수
- ItemData, Database 등 참조 시 항상 검증

### 2. 성능 고려
- 대량 아이템 처리 시 최적화 필요
- UI 갱신은 필요할 때만 수행
- 필터링/정렬은 LINQ 최적화 고려

### 3. 확장성
- 새로운 타입 추가 용이하도록 설계
- Excel 포맷 변경에 유연하게 대응
- UI 레이아웃 변경 용이하도록

### 4. 데이터 일관성
- Excel과 Unity Asset 동기화 중요
- itemKey 중복 체크 필수
- 참조 무결성 보장

### 5. UI 최적화
- 화면에 보이는 아이템만 렌더링 (가상화)
- 오브젝트 풀링 고려
- TextMeshPro 최적화 설정

---

## 다음 단계

아이템 시스템 완성 후:
1. **검술 세팅 화면 구현** (별도 계획서 작성 예정)
2. **세이브/로드 시스템 연동**
3. **전투 시스템과 연동** (장착 스탯 적용)
4. **상점 시스템** (선택 사항)
5. **제작 시스템** (선택 사항)

---

**문서 버전**: 1.1  
**작성일**: 2025년 10월 17일  
**최종 수정일**: 2025년 10월 20일  
**상태**: Phase 4 완료, Phase 5 대기 중

